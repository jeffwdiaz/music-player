<!-- This file (MainWindow.xaml) creates the main window you see when the app opens. Right now it just has a test button. -->
<Window x:Class="MusicPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Music Player" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <Button Name="TestButton" Content="Test AudioEngine with testfile.mp3" 
                    Click="TestButton_Click" 
                    Padding="10,5" 
                    FontSize="14"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10"/>
            <TextBlock Name="StatusText" Text="Click the button to test audio playback" 
                       HorizontalAlignment="Center" 
                       FontSize="12" 
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Width="400"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
